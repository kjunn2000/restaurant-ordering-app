import React,{useEffect,useSelector} from 'react'

const Auth = (SpecialComponent, option, isAdmin = null ) => {

    useEffect(()=> {
        
    })

    return (
        <SpecialComponent/>
    )
}

export default Auth
